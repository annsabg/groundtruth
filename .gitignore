__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store

# Raw source material stays local — never committed. The repo holds
# extracted structured records + citations, not the original documents.
# See docs/superpowers/specs/2026-08-25-groundtruth-extraction-pipeline-design.md §5.
*.pdf
*.docx
*.doc
sources-local/

# Subagent-driven-development workspace: ledgers, task briefs, review
# packages, and brainstorming's visual-companion server state. Local
# scratch, not part of the deliverable.
.superpowers/

# The site's local copy of the database, used only for `python3 -m
# http.server` local testing (see README's "Running the site locally").
# The deployed site assembles its own fresh copy at deploy time.
site/data/
